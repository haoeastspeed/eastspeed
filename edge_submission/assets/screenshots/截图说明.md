# 商店截图说明（Screenshots，可选，最多 6 张）

商店截图尺寸**必须严格为 1280×800 或 640×480**，本目录已生成的 4 张均为 **1280×800 PNG**，可直接在 Store Listings → Screenshots 上传（建议按下列顺序）：

| 文件 | 画面内容 |
|---|---|
| `shot_1_main.png` | 桌面程序主界面：分类侧栏、多任务、进度条、状态、实时速度曲线 |
| `shot_2_takeover.png` | 浏览器点击下载链接后自动弹出的接管确认窗（自动识别文件名/大小、16 连接、高级选项、哈希校验） |
| `shot_3_ext_install.png` | 桌面程序内置的“一键安装浏览器扩展”向导（自动探测 Chrome/Edge） |
| `shot_4_popup.png` | 扩展弹窗：接管/嗅探开关、媒体与 Blob 列表、手动下载、本地桥接 |

## 建议你在真实 Edge 里再补 2 张（更有说服力，凑满 6 张）

> 这两张含真实网页，AI 无法替你截取。截好后用任意工具裁成/缩放到 **1280×800**，命名 `shot_5_*.png`、`shot_6_*.png` 放入本目录。

1. **视频悬浮按钮**：装好扩展与桌面程序并显示“已连接”后，打开一个含 HTML5 视频（mp4）的网页并播放，截下视频右上角出现的“下载视频”悬浮按钮画面。
2. **媒体嗅探命中（已连接）**：在播放 m3u8/mp4 的页面打开扩展弹窗，截下右上角“已连接”、且“本页检测到的媒体”里列出真实媒体条目的画面。

截图要点：浏览器窗口内容区凑满 1280×800；避免出现个人账号、隐私标签页、书签栏敏感信息；中文界面即可。

> 截图为可选项，不提交也能过审；但 4–6 张高质量截图能显著提高安装转化率。
